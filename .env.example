# Used by Docker Compose. Generate a high-entropy local value; do not commit it.
SPORE_DB_PASSWORD="replace-with-a-random-local-secret"

# Optional private phone access through the dedicated Tailscale sidecar.
# Put the real key only in gitignored .env.compose, never in this file.
TS_AUTHKEY=""
